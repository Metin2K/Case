import {View, Text} from 'react-native';
import React from 'react';

const Favorites = () => {
  return (
    <View style={{paddingTop: 300}}>
      <Text style={{fontSize: 50, textAlign:'center'}}>Favorites</Text>
    </View>
  );
};

export default Favorites;
