/* import {configureStore} from '@reduxjs/toolkit';
import cartReducer from './CartSlice'; // Azaltıcıyı içe aktarın
import Cart from '../pages/Cart';

export default configureStore({
  reducer: {
    Cart: cartReducer, // Azaltıcıyı store'a ekleyin
  },
});
*/
